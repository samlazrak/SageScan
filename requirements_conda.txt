# Conda-specific requirements for ScanSage
# Use this file with: conda install --file requirements_conda.txt -c conda-forge

# Core scientific packages (better to install via conda)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.8.0

# Image processing (better via conda)
pillow>=10.0.0
opencv>=4.8.0

# Machine learning (better via conda)
pytorch>=2.0.0
transformers>=4.30.0

# NLP packages (better via conda)
nltk>=3.8.0
spacy>=3.6.0

# Other dependencies
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0 